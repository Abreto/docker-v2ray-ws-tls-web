[HOST]:80 {
  redir https://{host}{uri}
}

[HOST]:443 {
  log / stdout "{combined}"
  root /srv
  tls /private/cert.pem /private/key.pem
  proxy /theory v2ray:8008 {
    # websocket {
    header_upstream Connection {>Connection}
    header_upstream Upgrade "upgrade"
    # }
    header_upstream Host {>Host}
    header_upstream -Origin
  }
}
